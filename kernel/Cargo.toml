[package]
name = "hepos-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.6"
spin = "0.9"

[features]
default = []

[profile.dev]
panic = "abort"
opt-level = 1

[profile.release]
panic = "abort"
opt-level = 3
lto = true
codegen-units = 1
